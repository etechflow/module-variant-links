<?php
/** @var \Magento\Backend\Block\Template $block */
$saveUrl = $block->getUrl('etechflow_variantlinks/import/save');
$tplUrl  = $block->getUrl('etechflow_variantlinks/import/template');
?>
<div class="admin__page-section" style="max-width:820px">
    <div class="admin__page-section-title"><span class="title"><?= $block->escapeHtml(__('Bulk Import Variant Links')) ?></span></div>
    <p><?= $block->escapeHtml(__('Upload a CSV to set the "View Other" links on many products at once. Columns: sku, variant_options_url, variant_finishes_url, variant_sizes_url (include only the columns you need).')) ?></p>
    <ul style="margin:0 0 15px 18px;list-style:disc">
        <li><?= $block->escapeHtml(__('Match is by SKU. A blank cell leaves that link unchanged; put __CLEAR__ to remove a link.')) ?></li>
        <li><?= $block->escapeHtml(__('Tip: tick "Dry run" first to preview how many rows match before saving.')) ?></li>
        <li><a href="<?= $block->escapeUrl($tplUrl) ?>"><?= $block->escapeHtml(__('Download CSV template')) ?></a></li>
    </ul>
    <form action="<?= $block->escapeUrl($saveUrl) ?>" method="post" enctype="multipart/form-data">
        <?= /* @noEscape */ $block->getBlockHtml('formkey') ?>
        <div class="admin__field" style="margin-bottom:12px">
            <label class="admin__field-label" style="display:inline-block;width:auto;margin-right:10px"><?= $block->escapeHtml(__('CSV file')) ?></label>
            <input type="file" name="import_file" accept=".csv,text/csv" required/>
        </div>
        <div class="admin__field" style="margin-bottom:16px">
            <label><input type="checkbox" name="dry_run" value="1" checked/> <?= $block->escapeHtml(__('Dry run (preview only — do not save)')) ?></label>
        </div>
        <button type="submit" class="action-primary"><span><?= $block->escapeHtml(__('Upload & Apply')) ?></span></button>
    </form>
</div>
